 git push --set-upstream origin rf/messaging
 
 git config alias.acp '! acp() { git add . && git add -A && git commit -a -m "$1" && git push ; } ; acp'

 git config alias.acp '! acp() { git add . && git add -A && git commit -m "$1" && git push ; } ; acp'


 depois, é só executar:
 git acp "message"

Error: error:0308010C:digital envelope routines::unsupported

set NODE_OPTIONS=--openssl-legacy-provider
ou
$env:NODE_OPTIONS = "--openssl-legacy-provider"

// Nice icons
https://www.svgrepo.com/